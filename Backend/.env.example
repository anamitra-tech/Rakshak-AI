# Copy this file to .env and fill in real values.
# The app runs fine with all of these blank.

# Unused for now -- present so the real classifier file (see
# app/services/classifier_placeholder.py) can read them once it's dropped in.
GEMINI_API_KEY=
GROQ_API_KEY=

MAPBOX_TOKEN=

FRONTEND_ORIGIN=http://localhost:5173

# --- Auth ---
GOOGLE_CLIENT_ID=
SESSION_SECRET=dev-insecure-secret-change-me
